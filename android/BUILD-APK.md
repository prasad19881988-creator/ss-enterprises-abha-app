Open this `android` folder in Android Studio.
Select Build > Generate App Bundles or APKs > Generate APK(s).
For a release build, use Build > Generate Signed Bundle / APK and create/use your signing key.
